--- new-nav-debian.orig/setup.py
+++ new-nav-debian/setup.py
@@ -1,3 +1,4 @@
+import re
 import os
 import subprocess
 from glob import glob
@@ -28,7 +29,7 @@ build.sub_commands = [('build_sass', Non
 setup(
     setup_requires=['libsass', 'setuptools_scm'],
     python_requires=">=2.7",
-    use_scm_version=True,
+    use_scm_version={'tag_regex': re.compile(r'(debian/)?(?P<version>[0-9.b]+)')},
 
     name="nav",
     author="Uninett AS",
